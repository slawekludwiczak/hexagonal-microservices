spring:
  application:
    name: priceflow-offer-scraper
  config:
    import:
      - classpath:persistence.yml
      - classpath:api.yml
      - classpath:events.yml
      - classpath:discovery.yml
logging:
  level:
    com:
      ludigi:
        priceflow:
          offer: INFO
